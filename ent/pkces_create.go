// Code generated by ent, DO NOT EDIT.

package ent

import (
	"authorization-service/ent/pkces"
	"authorization-service/ent/request"
	"context"
	"fmt"

	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// PKCESCreate is the builder for creating a PKCES entity.
type PKCESCreate struct {
	config
	mutation *PKCESMutation
	hooks    []Hook
}

// SetID sets the "id" field.
func (pc *PKCESCreate) SetID(s string) *PKCESCreate {
	pc.mutation.SetID(s)
	return pc
}

// SetRequestIDID sets the "request_id" edge to the Request entity by ID.
func (pc *PKCESCreate) SetRequestIDID(id string) *PKCESCreate {
	pc.mutation.SetRequestIDID(id)
	return pc
}

// SetNillableRequestIDID sets the "request_id" edge to the Request entity by ID if the given value is not nil.
func (pc *PKCESCreate) SetNillableRequestIDID(id *string) *PKCESCreate {
	if id != nil {
		pc = pc.SetRequestIDID(*id)
	}
	return pc
}

// SetRequestID sets the "request_id" edge to the Request entity.
func (pc *PKCESCreate) SetRequestID(r *Request) *PKCESCreate {
	return pc.SetRequestIDID(r.ID)
}

// Mutation returns the PKCESMutation object of the builder.
func (pc *PKCESCreate) Mutation() *PKCESMutation {
	return pc.mutation
}

// Save creates the PKCES in the database.
func (pc *PKCESCreate) Save(ctx context.Context) (*PKCES, error) {
	return withHooks(ctx, pc.sqlSave, pc.mutation, pc.hooks)
}

// SaveX calls Save and panics if Save returns an error.
func (pc *PKCESCreate) SaveX(ctx context.Context) *PKCES {
	v, err := pc.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pc *PKCESCreate) Exec(ctx context.Context) error {
	_, err := pc.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pc *PKCESCreate) ExecX(ctx context.Context) {
	if err := pc.Exec(ctx); err != nil {
		panic(err)
	}
}

// check runs all checks and user-defined validators on the builder.
func (pc *PKCESCreate) check() error {
	return nil
}

func (pc *PKCESCreate) sqlSave(ctx context.Context) (*PKCES, error) {
	if err := pc.check(); err != nil {
		return nil, err
	}
	_node, _spec := pc.createSpec()
	if err := sqlgraph.CreateNode(ctx, pc.driver, _spec); err != nil {
		if sqlgraph.IsConstraintError(err) {
			err = &ConstraintError{msg: err.Error(), wrap: err}
		}
		return nil, err
	}
	if _spec.ID.Value != nil {
		if id, ok := _spec.ID.Value.(string); ok {
			_node.ID = id
		} else {
			return nil, fmt.Errorf("unexpected PKCES.ID type: %T", _spec.ID.Value)
		}
	}
	pc.mutation.id = &_node.ID
	pc.mutation.done = true
	return _node, nil
}

func (pc *PKCESCreate) createSpec() (*PKCES, *sqlgraph.CreateSpec) {
	var (
		_node = &PKCES{config: pc.config}
		_spec = sqlgraph.NewCreateSpec(pkces.Table, sqlgraph.NewFieldSpec(pkces.FieldID, field.TypeString))
	)
	if id, ok := pc.mutation.ID(); ok {
		_node.ID = id
		_spec.ID.Value = id
	}
	if nodes := pc.mutation.RequestIDIDs(); len(nodes) > 0 {
		edge := &sqlgraph.EdgeSpec{
			Rel:     sqlgraph.O2O,
			Inverse: true,
			Table:   pkces.RequestIDTable,
			Columns: []string{pkces.RequestIDColumn},
			Bidi:    false,
			Target: &sqlgraph.EdgeTarget{
				IDSpec: sqlgraph.NewFieldSpec(request.FieldID, field.TypeString),
			},
		}
		for _, k := range nodes {
			edge.Target.Nodes = append(edge.Target.Nodes, k)
		}
		_node.request_pkce = &nodes[0]
		_spec.Edges = append(_spec.Edges, edge)
	}
	return _node, _spec
}

// PKCESCreateBulk is the builder for creating many PKCES entities in bulk.
type PKCESCreateBulk struct {
	config
	builders []*PKCESCreate
}

// Save creates the PKCES entities in the database.
func (pcb *PKCESCreateBulk) Save(ctx context.Context) ([]*PKCES, error) {
	specs := make([]*sqlgraph.CreateSpec, len(pcb.builders))
	nodes := make([]*PKCES, len(pcb.builders))
	mutators := make([]Mutator, len(pcb.builders))
	for i := range pcb.builders {
		func(i int, root context.Context) {
			builder := pcb.builders[i]
			var mut Mutator = MutateFunc(func(ctx context.Context, m Mutation) (Value, error) {
				mutation, ok := m.(*PKCESMutation)
				if !ok {
					return nil, fmt.Errorf("unexpected mutation type %T", m)
				}
				if err := builder.check(); err != nil {
					return nil, err
				}
				builder.mutation = mutation
				var err error
				nodes[i], specs[i] = builder.createSpec()
				if i < len(mutators)-1 {
					_, err = mutators[i+1].Mutate(root, pcb.builders[i+1].mutation)
				} else {
					spec := &sqlgraph.BatchCreateSpec{Nodes: specs}
					// Invoke the actual operation on the latest mutation in the chain.
					if err = sqlgraph.BatchCreate(ctx, pcb.driver, spec); err != nil {
						if sqlgraph.IsConstraintError(err) {
							err = &ConstraintError{msg: err.Error(), wrap: err}
						}
					}
				}
				if err != nil {
					return nil, err
				}
				mutation.id = &nodes[i].ID
				mutation.done = true
				return nodes[i], nil
			})
			for i := len(builder.hooks) - 1; i >= 0; i-- {
				mut = builder.hooks[i](mut)
			}
			mutators[i] = mut
		}(i, ctx)
	}
	if len(mutators) > 0 {
		if _, err := mutators[0].Mutate(ctx, pcb.builders[0].mutation); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

// SaveX is like Save, but panics if an error occurs.
func (pcb *PKCESCreateBulk) SaveX(ctx context.Context) []*PKCES {
	v, err := pcb.Save(ctx)
	if err != nil {
		panic(err)
	}
	return v
}

// Exec executes the query.
func (pcb *PKCESCreateBulk) Exec(ctx context.Context) error {
	_, err := pcb.Save(ctx)
	return err
}

// ExecX is like Exec, but panics if an error occurs.
func (pcb *PKCESCreateBulk) ExecX(ctx context.Context) {
	if err := pcb.Exec(ctx); err != nil {
		panic(err)
	}
}
