// Code generated by ent, DO NOT EDIT.

package ent

import (
	"authorization-service/ent/accesstokens"
	"authorization-service/ent/clients"
	"authorization-service/ent/predicate"
	"authorization-service/ent/session"
	"context"
	"fmt"
	"math"

	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
)

// AccessTokensQuery is the builder for querying AccessTokens entities.
type AccessTokensQuery struct {
	config
	ctx           *QueryContext
	order         []accesstokens.OrderOption
	inters        []Interceptor
	predicates    []predicate.AccessTokens
	withClientID  *ClientsQuery
	withSessionID *SessionQuery
	withFKs       bool
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the AccessTokensQuery builder.
func (atq *AccessTokensQuery) Where(ps ...predicate.AccessTokens) *AccessTokensQuery {
	atq.predicates = append(atq.predicates, ps...)
	return atq
}

// Limit the number of records to be returned by this query.
func (atq *AccessTokensQuery) Limit(limit int) *AccessTokensQuery {
	atq.ctx.Limit = &limit
	return atq
}

// Offset to start from.
func (atq *AccessTokensQuery) Offset(offset int) *AccessTokensQuery {
	atq.ctx.Offset = &offset
	return atq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (atq *AccessTokensQuery) Unique(unique bool) *AccessTokensQuery {
	atq.ctx.Unique = &unique
	return atq
}

// Order specifies how the records should be ordered.
func (atq *AccessTokensQuery) Order(o ...accesstokens.OrderOption) *AccessTokensQuery {
	atq.order = append(atq.order, o...)
	return atq
}

// QueryClientID chains the current query on the "client_id" edge.
func (atq *AccessTokensQuery) QueryClientID() *ClientsQuery {
	query := (&ClientsClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(accesstokens.Table, accesstokens.FieldID, selector),
			sqlgraph.To(clients.Table, clients.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, accesstokens.ClientIDTable, accesstokens.ClientIDColumn),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// QuerySessionID chains the current query on the "session_id" edge.
func (atq *AccessTokensQuery) QuerySessionID() *SessionQuery {
	query := (&SessionClient{config: atq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := atq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := atq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(accesstokens.Table, accesstokens.FieldID, selector),
			sqlgraph.To(session.Table, session.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, accesstokens.SessionIDTable, accesstokens.SessionIDColumn),
		)
		fromU = sqlgraph.SetNeighbors(atq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first AccessTokens entity from the query.
// Returns a *NotFoundError when no AccessTokens was found.
func (atq *AccessTokensQuery) First(ctx context.Context) (*AccessTokens, error) {
	nodes, err := atq.Limit(1).All(setContextOp(ctx, atq.ctx, "First"))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{accesstokens.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (atq *AccessTokensQuery) FirstX(ctx context.Context) *AccessTokens {
	node, err := atq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first AccessTokens ID from the query.
// Returns a *NotFoundError when no AccessTokens ID was found.
func (atq *AccessTokensQuery) FirstID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = atq.Limit(1).IDs(setContextOp(ctx, atq.ctx, "FirstID")); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{accesstokens.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (atq *AccessTokensQuery) FirstIDX(ctx context.Context) string {
	id, err := atq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single AccessTokens entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one AccessTokens entity is found.
// Returns a *NotFoundError when no AccessTokens entities are found.
func (atq *AccessTokensQuery) Only(ctx context.Context) (*AccessTokens, error) {
	nodes, err := atq.Limit(2).All(setContextOp(ctx, atq.ctx, "Only"))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{accesstokens.Label}
	default:
		return nil, &NotSingularError{accesstokens.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (atq *AccessTokensQuery) OnlyX(ctx context.Context) *AccessTokens {
	node, err := atq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only AccessTokens ID in the query.
// Returns a *NotSingularError when more than one AccessTokens ID is found.
// Returns a *NotFoundError when no entities are found.
func (atq *AccessTokensQuery) OnlyID(ctx context.Context) (id string, err error) {
	var ids []string
	if ids, err = atq.Limit(2).IDs(setContextOp(ctx, atq.ctx, "OnlyID")); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{accesstokens.Label}
	default:
		err = &NotSingularError{accesstokens.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (atq *AccessTokensQuery) OnlyIDX(ctx context.Context) string {
	id, err := atq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of AccessTokensSlice.
func (atq *AccessTokensQuery) All(ctx context.Context) ([]*AccessTokens, error) {
	ctx = setContextOp(ctx, atq.ctx, "All")
	if err := atq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*AccessTokens, *AccessTokensQuery]()
	return withInterceptors[[]*AccessTokens](ctx, atq, qr, atq.inters)
}

// AllX is like All, but panics if an error occurs.
func (atq *AccessTokensQuery) AllX(ctx context.Context) []*AccessTokens {
	nodes, err := atq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of AccessTokens IDs.
func (atq *AccessTokensQuery) IDs(ctx context.Context) (ids []string, err error) {
	if atq.ctx.Unique == nil && atq.path != nil {
		atq.Unique(true)
	}
	ctx = setContextOp(ctx, atq.ctx, "IDs")
	if err = atq.Select(accesstokens.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (atq *AccessTokensQuery) IDsX(ctx context.Context) []string {
	ids, err := atq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (atq *AccessTokensQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, atq.ctx, "Count")
	if err := atq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, atq, querierCount[*AccessTokensQuery](), atq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (atq *AccessTokensQuery) CountX(ctx context.Context) int {
	count, err := atq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (atq *AccessTokensQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, atq.ctx, "Exist")
	switch _, err := atq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (atq *AccessTokensQuery) ExistX(ctx context.Context) bool {
	exist, err := atq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the AccessTokensQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (atq *AccessTokensQuery) Clone() *AccessTokensQuery {
	if atq == nil {
		return nil
	}
	return &AccessTokensQuery{
		config:        atq.config,
		ctx:           atq.ctx.Clone(),
		order:         append([]accesstokens.OrderOption{}, atq.order...),
		inters:        append([]Interceptor{}, atq.inters...),
		predicates:    append([]predicate.AccessTokens{}, atq.predicates...),
		withClientID:  atq.withClientID.Clone(),
		withSessionID: atq.withSessionID.Clone(),
		// clone intermediate query.
		sql:  atq.sql.Clone(),
		path: atq.path,
	}
}

// WithClientID tells the query-builder to eager-load the nodes that are connected to
// the "client_id" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AccessTokensQuery) WithClientID(opts ...func(*ClientsQuery)) *AccessTokensQuery {
	query := (&ClientsClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withClientID = query
	return atq
}

// WithSessionID tells the query-builder to eager-load the nodes that are connected to
// the "session_id" edge. The optional arguments are used to configure the query builder of the edge.
func (atq *AccessTokensQuery) WithSessionID(opts ...func(*SessionQuery)) *AccessTokensQuery {
	query := (&SessionClient{config: atq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	atq.withSessionID = query
	return atq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		RequestID string `json:"request_id,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.AccessTokens.Query().
//		GroupBy(accesstokens.FieldRequestID).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (atq *AccessTokensQuery) GroupBy(field string, fields ...string) *AccessTokensGroupBy {
	atq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &AccessTokensGroupBy{build: atq}
	grbuild.flds = &atq.ctx.Fields
	grbuild.label = accesstokens.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		RequestID string `json:"request_id,omitempty"`
//	}
//
//	client.AccessTokens.Query().
//		Select(accesstokens.FieldRequestID).
//		Scan(ctx, &v)
func (atq *AccessTokensQuery) Select(fields ...string) *AccessTokensSelect {
	atq.ctx.Fields = append(atq.ctx.Fields, fields...)
	sbuild := &AccessTokensSelect{AccessTokensQuery: atq}
	sbuild.label = accesstokens.Label
	sbuild.flds, sbuild.scan = &atq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a AccessTokensSelect configured with the given aggregations.
func (atq *AccessTokensQuery) Aggregate(fns ...AggregateFunc) *AccessTokensSelect {
	return atq.Select().Aggregate(fns...)
}

func (atq *AccessTokensQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range atq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, atq); err != nil {
				return err
			}
		}
	}
	for _, f := range atq.ctx.Fields {
		if !accesstokens.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if atq.path != nil {
		prev, err := atq.path(ctx)
		if err != nil {
			return err
		}
		atq.sql = prev
	}
	return nil
}

func (atq *AccessTokensQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*AccessTokens, error) {
	var (
		nodes       = []*AccessTokens{}
		withFKs     = atq.withFKs
		_spec       = atq.querySpec()
		loadedTypes = [2]bool{
			atq.withClientID != nil,
			atq.withSessionID != nil,
		}
	)
	if atq.withClientID != nil || atq.withSessionID != nil {
		withFKs = true
	}
	if withFKs {
		_spec.Node.Columns = append(_spec.Node.Columns, accesstokens.ForeignKeys...)
	}
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*AccessTokens).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &AccessTokens{config: atq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, atq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := atq.withClientID; query != nil {
		if err := atq.loadClientID(ctx, query, nodes, nil,
			func(n *AccessTokens, e *Clients) { n.Edges.ClientID = e }); err != nil {
			return nil, err
		}
	}
	if query := atq.withSessionID; query != nil {
		if err := atq.loadSessionID(ctx, query, nodes, nil,
			func(n *AccessTokens, e *Session) { n.Edges.SessionID = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (atq *AccessTokensQuery) loadClientID(ctx context.Context, query *ClientsQuery, nodes []*AccessTokens, init func(*AccessTokens), assign func(*AccessTokens, *Clients)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*AccessTokens)
	for i := range nodes {
		if nodes[i].clients_access_token == nil {
			continue
		}
		fk := *nodes[i].clients_access_token
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(clients.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "clients_access_token" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}
func (atq *AccessTokensQuery) loadSessionID(ctx context.Context, query *SessionQuery, nodes []*AccessTokens, init func(*AccessTokens), assign func(*AccessTokens, *Session)) error {
	ids := make([]string, 0, len(nodes))
	nodeids := make(map[string][]*AccessTokens)
	for i := range nodes {
		if nodes[i].session_access_token == nil {
			continue
		}
		fk := *nodes[i].session_access_token
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(session.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "session_access_token" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (atq *AccessTokensQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := atq.querySpec()
	_spec.Node.Columns = atq.ctx.Fields
	if len(atq.ctx.Fields) > 0 {
		_spec.Unique = atq.ctx.Unique != nil && *atq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, atq.driver, _spec)
}

func (atq *AccessTokensQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(accesstokens.Table, accesstokens.Columns, sqlgraph.NewFieldSpec(accesstokens.FieldID, field.TypeString))
	_spec.From = atq.sql
	if unique := atq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if atq.path != nil {
		_spec.Unique = true
	}
	if fields := atq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, accesstokens.FieldID)
		for i := range fields {
			if fields[i] != accesstokens.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
	}
	if ps := atq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := atq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := atq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := atq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (atq *AccessTokensQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(atq.driver.Dialect())
	t1 := builder.Table(accesstokens.Table)
	columns := atq.ctx.Fields
	if len(columns) == 0 {
		columns = accesstokens.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if atq.sql != nil {
		selector = atq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if atq.ctx.Unique != nil && *atq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range atq.predicates {
		p(selector)
	}
	for _, p := range atq.order {
		p(selector)
	}
	if offset := atq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := atq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// AccessTokensGroupBy is the group-by builder for AccessTokens entities.
type AccessTokensGroupBy struct {
	selector
	build *AccessTokensQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (atgb *AccessTokensGroupBy) Aggregate(fns ...AggregateFunc) *AccessTokensGroupBy {
	atgb.fns = append(atgb.fns, fns...)
	return atgb
}

// Scan applies the selector query and scans the result into the given value.
func (atgb *AccessTokensGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, atgb.build.ctx, "GroupBy")
	if err := atgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AccessTokensQuery, *AccessTokensGroupBy](ctx, atgb.build, atgb, atgb.build.inters, v)
}

func (atgb *AccessTokensGroupBy) sqlScan(ctx context.Context, root *AccessTokensQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(atgb.fns))
	for _, fn := range atgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*atgb.flds)+len(atgb.fns))
		for _, f := range *atgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*atgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := atgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// AccessTokensSelect is the builder for selecting fields of AccessTokens entities.
type AccessTokensSelect struct {
	*AccessTokensQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (ats *AccessTokensSelect) Aggregate(fns ...AggregateFunc) *AccessTokensSelect {
	ats.fns = append(ats.fns, fns...)
	return ats
}

// Scan applies the selector query and scans the result into the given value.
func (ats *AccessTokensSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, ats.ctx, "Select")
	if err := ats.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*AccessTokensQuery, *AccessTokensSelect](ctx, ats.AccessTokensQuery, ats, ats.inters, v)
}

func (ats *AccessTokensSelect) sqlScan(ctx context.Context, root *AccessTokensQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(ats.fns))
	for _, fn := range ats.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*ats.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := ats.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
